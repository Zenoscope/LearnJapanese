
/// @description Insert description here
// You can write your code in this editor

/// @description Insert description here
// You can write your code in this editor

draw_self();

/*
if (moving) {
	//show_debug_message("move the card back");
	if ( distance_to_point(end_x_loc,end_y_loc) > 5 ){
			move_step = 5;
			move_towards_point(end_x_loc,end_y_loc,move_step);
			interpolation_amount = (end_y_loc - y) / move_step;
			
			// Need to figure out how far in % the image has moved between
			// start and end points.
			start = 400
			end = 50
			end - start / 100
			
			
			//self.image_xscale = lerp(start_x_scale, end_x_scale, interpolation_amount);
			//self.image_xscale = lerp(start_y_scale, end_y_scale, interpolation_amount);
		}
	else {
		//show_debug_message(btn_name +" Stop!");
		x = end_x_loc;
        y = end_y_loc;
		//show_debug_message( string (x_origin) +  string( y_origin ) );
		moving = false;
		speed = 0;
		depth = -200;	
		}
}
*/
